{
  "name": "aquila-mern-aws-deployment",
  "version": "1.0.0",
  "description": "MERN stack app with AWS deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run start\" \"npm run client\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}